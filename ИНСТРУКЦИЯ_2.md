# Instruction 2 (updated)

## What was changed

- Image file names were renamed to a unified `img_*` style.
- Main entrypoint name was normalized back to `main.py` in practice folders.
- References in Python scripts and docs were updated to match new names.
- Absolute Windows image paths in scripts were replaced with relative/default paths where needed.
- Practice 2 and 3 startup behavior was improved to avoid silent waiting in terminal.

## What to verify

- Each practice folder has a runnable entry file:
  - main flow: semantic script name (see `README.md`)
  - extra/demo flow: additional semantic demo scripts
- Default image path in each OpenCV script points to an existing `img_*` file in the same folder.
- Deep learning practices (`8`, `9`, `10`) still run with required dependencies.

## Submission checklist

1. Run all practices from `README.md` using new script names.
2. Keep test images near scripts (or pass custom paths via `--image` where supported).
3. For `10/main.py`, reduce dataset sizes on weak hardware if needed.
4. Add screenshots and short conclusions for each practice.

